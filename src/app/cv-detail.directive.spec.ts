import { CvDetailDirective } from './cv-detail.directive';

describe('CvDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new CvDetailDirective();
    expect(directive).toBeTruthy();
  });
});
