import { CvItemDirective } from './cv-item.directive';

describe('CvItemDirective', () => {
  it('should create an instance', () => {
    const directive = new CvItemDirective();
    expect(directive).toBeTruthy();
  });
});
